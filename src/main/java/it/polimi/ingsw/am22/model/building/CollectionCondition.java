package it.polimi.ingsw.am22.model.building;

/**
 * The condition a {@link CollectionRewardEffect} checks in order to grant its
 * food reward.
 */
public enum CollectionCondition {
    /** A complete set of all six character types. */
    SET_OF_6,
    /** A pair of Inventors sharing the same icon. */
    INVENTOR_PAIR
}
